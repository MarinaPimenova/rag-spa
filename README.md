# ReactJS Chat Client (Vite + React)

##  Setup Frontend Project

```shell
# Create project
npm create vite@latest rag-spa -- --template react

cd rag-spa

# Install dependencies
npm install

# Add UUID library for sessions
npm install uuid

```


